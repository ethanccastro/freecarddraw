{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setup-env",
            "type": "shell",
            "command": "cd ${workspaceFolder} && if [ -f .env.local ]; then export $(cat .env.local | xargs); fi",
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": []
        }
    ]
} 